import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.spacenova.app',
  appName: 'spacenova',
  webDir: 'dist/spacenova/browser'
};

export default config;
